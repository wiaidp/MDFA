# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
cache: packages
- Rscript -e 'install.packages("devtools");devtools::install_deps("MDFA")'
install:
  - Rscript -e 'devtools::install_deps("MDFA")'
script:
  - Rscript -e 'devtools::check("MDFA")'
